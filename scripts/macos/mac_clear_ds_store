#!/bin/bash
# ----------------------------------------------------------------------------------------------------------
# Description:		Delete unwanted .DS_Store files
# Author:		Mridul Ranjan
# Platform(s):		Darwin
# ----------------------------------------------------------------------------------------------------------
# Version:		1.0
# Last Update:
# First Draft:		2021-07-08
# ----------------------------------------------------------------------------------------------------------

# ----------------------------------------------------------------------------------------------------------
# FUNCTIONS
# ----------------------------------------------------------------------------------------------------------
function platform_check()
{
	echo $allowed_kernels | grep -w $kernel > /dev/null 2>&1
	if [ $? -ne 0 ]; then
		echo "Invalid OS Platform"
		exit 1
	fi
}

function clear_ds_store()
{
	for dir_name in ${dir_array[@]}; do
		if [[ ! -d $dir_name ]]; then
        		echo "$dir_name not found"
		else
			echo "[$dir_name]"
			find $dir_name -type f -name .DS_Store -exec rm -v {} \;
		fi
		echo
	done
}

# -----------------------------------
# VARIABLES | Fixed
# -----------------------------------
allowed_kernels="Darwin"
Linux_Home_Base="/home"
Darwin_Home_Base="/Users"

# -----------------------------------
# VARIABLES | Evaulate
# -----------------------------------
kernel=$(uname)
run_user=$(id -un)
dir_array="$HOME/Documents $HOME/Tech"

# ----------------------------------------------------------------------------------------------------------
# MAIN
# ----------------------------------------------------------------------------------------------------------
platform_check
clear_ds_store

# ----------------------------------------------------------------------------------------------------------
# EOF
# ----------------------------------------------------------------------------------------------------------
